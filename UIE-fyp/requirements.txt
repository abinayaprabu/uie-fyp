# UIE-fyp — Underwater Image Quality Analysis (UIEB + handcrafted features + CNN)
# Install:  pip install -r requirements.txt
# For a small CPU-only PyTorch (~200 MB instead of ~2 GB CUDA bundle), use:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# then: pip install -r requirements.txt
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scikit-image>=0.21
opencv-python-headless>=4.8  # headless server build (same API incl. SIFT; needs no libGL)
scipy>=1.11
matplotlib>=3.7
tqdm>=4.66
torch>=2.0
gdown>=5.0
